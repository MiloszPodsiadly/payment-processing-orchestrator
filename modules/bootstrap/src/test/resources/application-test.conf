payment {
  application {
    name = "payment-processing-orchestrator-test"
    environment = "test"
  }

  http {
    interface = "127.0.0.1"
    port = 8081
  }

  cassandra {
    host = "127.0.0.1"
    port = 9042
    local-datacenter = "datacenter1"
  }

  security {
    token-issuer = "test-issuer"
  }

  observability {
    structured-logging = false
  }

  provider {
    mode = "mock"
  }
}
